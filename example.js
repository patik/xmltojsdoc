// All three comment blocks in here should result in the same JSDoc output (except the number in the summary/description)

var ignoreMe1;


///<summary>Example 1 description</summary>
///<param name="elem" type="Element">Button element</param>
///<param name="options" type="Object">Optional settings</param>
///<returns>True if successful, false if unsuccessful</returns>
///<remark>If no `elem` is specified, the first `.button` on the page will be used</remark>

var ignoreMe2;

/**
 * Adds event listeners to a particular tooltip
 * @param   {Element}  tooltipElem  The div.tooltip
 * @param   {Array}    links        Links within the tooltip (optional)
 * @return  {[type]}                The target element
 */

var ignoreMe3;

    ///<summary>Example 2 description</summary>
    ///<param name="elem" type="Element">Button element</param>
    ///<param name="options" type="Object">Optional settings</param>
    ///<returns>True if successful, false if unsuccessful</returns>
    ///<remark>If no `elem` is specified, the first `.button` on the page will be used</remark>

///<summary>Example 3 description</summary>
///<remark>If no `elem` is specified, the first `.button` on the page will be used</remark>
///<param name="elem" type="Element">Button element</param>
///<param name="options" type="Object">Optional settings</param>
///<returns>True if successful, false if unsuccessful</returns>

var ignoreMe4;
